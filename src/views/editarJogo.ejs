<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Jogo - Professor</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Estilo para alinhar o checkbox de forma limpa */
        .checkbox-group {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }
        /* Ajuste do label para o checkbox */
        .checkbox-group label {
            margin-bottom: 0;
            font-weight: normal; /* Não precisa de negrito para o label do checkbox */
        }
        .checkbox-group input[type="checkbox"] {
            width: 20px;
            height: 20px;
            margin-right: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Editar Jogo</h1>

    <div id="area-edicao-jogo" class="container">
        
        <h2>Editando: <%= jogo.nome_jogo %></h2>
        
        <form id="form-editar-jogo" method="POST">
            
            <div class="form-group">
                <label for="nome_jogo">Nome do Jogo:</label>
                <input type="text" id="nome_jogo" name="nome_jogo" value="<%= jogo.nome_jogo %>" required>
            </div>

            <div class="form-group">
                <label for="descricao">Descrição:</label>
                <textarea id="descricao" name="descricao" rows="4"><%= jogo.descricao %></textarea>
            </div>
            
            <hr>

            <div class="form-group checkbox-group">
                <input type="checkbox" id="ativo" name="ativo" <%= jogo.ativo ? "checked" : "" %>>
                <label for="ativo">Ativo</label>
            </div>

            <button type="submit" class="btn">Salvar Alterações</button>
        </form>
        
        <div class="link-secundario">
            <p><a href="/jogos/admin">Cancelar e Voltar</a></p>
        </div>
    </div>
</body>
</html>