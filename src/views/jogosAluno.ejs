<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu de Jogos - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Estilos específicos para o catálogo de jogos */
        .game-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-top: 30px;
            max-width: 600px;
            margin: 30px auto;
            padding: 0 20px;
        }

        .game-card {
            background-color: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 250px; 
            border-top: 5px solid #4CAF50; 
        }

        .game-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .game-card h3 {
            color: #00796b;
            margin-top: 0;
            font-size: 1.5rem;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
            margin-bottom: 10px;
        }

        .game-card p {
            color: #555;
            font-size: 1rem;
            flex-grow: 1; 
        }
        
 
        .game-card .btn {
            margin-top: 10px; 
            width: 100%;
            padding: 8px 0px; 
            font-size: 1rem; 
        }

       

        /* Estilo para quando não houver jogos.*/
        .empty-state {
            text-align: center;
            padding: 50px;
            background-color: #fff;
            border-radius: 12px;
            max-width: px;
            margin: 50px auto;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
        }
      
    </style>
</head>
<body>
    <div style="text-align: center; margin-top: 20px;">
        <h1>Menu de Jogos</h1>
        <p>Escolha um jogo abaixo para começar o aprendizado!</p>
    </div>

    <% if (jogos.length === 0) { %>
        <div class="empty-state">
            <h3>Nenhum jogo ativo no momento.</h3>
            <p>Seu professor ainda não liberou jogos para sua turma. Volte em breve!</p>
            <a href="/dashboard" class="btn btn-secondary" style="width: auto;">Voltar ao Painel</a>
        </div>
    <% } else { %>
        <div class="game-grid">
            <% jogos.forEach(jogo => { %>
                <div class="game-card">
                    <div>
                        <h3><%= jogo.nome_jogo %></h3>
                        <p><%= jogo.descricao %></p>
                    </div>
                    <div>
                    <a href="/jogos/<%= jogo.id_jogo %>" class="btn">Jogar Agora</a>
                    <a href="/dashboard" class="btn btn-secondary">Voltar ao Painel</a>
                    </div>
                </div>
            <% }) %>
        </div>
        
    <% } %>

</body>
</html>