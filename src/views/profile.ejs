<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Perfil - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Estilo para a imagem do avatar dentro do placeholder */
        .avatar-placeholder img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }
    </style>
</head>
<body>

    <div id="conteudo-perfil" class="container content-area">
        <h2>Meu Perfil</h2>
        
        <div class="form-group avatar-section">
            <label>Avatar:</label>
            
            <div class="avatar-placeholder">
                <img src="<%= avatarUrl %>" alt="Avatar do Aluno">
            </div>
            
            <a href="/perfil/novo-avatar" class="btn btn-small">Trocar Avatar</a>
        </div>

        <div class="form-group">
            <label for="perfil-nome">Nome (Usuário):</label>
            <input type="text" id="perfil-nome" value="<%= username %>" readonly>
        </div>

        <div class="form-group-inline" style="display: flex; gap: 20px;">
            <div class="form-group" style="flex: 1;">
                <label for="perfil-idade">Idade:</label>
                <input type="text" id="perfil-idade" value="<%= idade %> anos" readonly>
            </div>
            <div class="form-group" style="flex: 1;">
                <label for="perfil-turma">Turma:</label>
                <input type="text" id="perfil-turma" value="<%= turma %>" readonly>
            </div>
        </div>

        <div class="profile-stats" style="display: flex; justify-content: space-around; background: #f9f9f9; border-radius: 8px; padding: 15px; margin-bottom: 20px;">
            </div>

        <form method="POST" action="/perfil/atualizar">

            <div class="form-group">
                <label for="perfil-descricao">Minha Descrição:</label>
                
                <textarea id="perfil-descricao" name="descricao" rows="4" placeholder="Fale um pouco sobre você..."><%= descricao %></textarea>
            </div>
            
            <div class="user-actions">
                <button type="submit" id="botao-salvar-perfil" class="btn">Salvar Alterações</button>
                <a href="/dashboard" id="botao-cancelar" class="btn btn-secondary">Cancelar</a>
            </div>

        </form>
        </div>

</body>
</html>